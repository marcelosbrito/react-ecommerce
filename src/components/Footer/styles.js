import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    
    footer: {
        width: 'auto',
        padding: '10px',
        height: '20px',
        display: 'flex',
        justifyContent: 'center',
        align: 'center',
        fontSize: '0.9rem',
        fontWeight: "bold",
        backgroundImage: 'linear-gradient(to bottom right, #ff6200, #ffb400)',
        position:'relative',
        left:'0',
        bottom:'0',
        right:'0'
    }

}));